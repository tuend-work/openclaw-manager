{
    "auth": {
        "profiles": {
            "openrouter:default": {
                "provider": "openrouter",
                "mode": "api_key"
            }
        }
    },
    "gateway": {
        "port": 18789,
        "mode": "local",
        "bind": "auto",
        "controlUi": {
            "allowedOrigins": [
                "https://${DOMAIN_NAME}"
            ]
        },
        "auth": {
            "mode": "token",
            "token": "${OPENCLAW_GATEWAY_TOKEN}"
        },
        "tailscale": {
            "mode": "off",
            "resetOnExit": false
        },
        "remote": {
            "token": "${OPENCLAW_GATEWAY_TOKEN}"
        },
        "nodes": {
            "denyCommands": [
                "camera.snap",
                "camera.clip",
                "screen.record",
                "contacts.add",
                "calendar.add",
                "reminders.add",
                "sms.send"
            ]
        }
    },
    "agents": {
        "defaults": {
            "model": {
                "primary": "openrouter/auto"
            },
            "models": {
                "openrouter/auto": {
                    "alias": "OpenRouter"
                }
            },
            "workspace": "/root/.openclaw/workspace"
        },
        "list": [
            { "id": "main", "workspace": "/root/.openclaw/workspace-main" }
        ]
    },
    "bindings": [
        { "agentId": "main", "match": { "channel": "telegram", "accountId": "default" } }
    ],
    "channels": {
        "telegram": {
            "enabled": true,
            "accounts": {
                "default": {
                    "botToken": "${TELEGRAM_BOT_TOKEN}",
                    "dmPolicy": "pairing",
                    "allowFrom": [
                        "${TELEGRAM_ALLOW_USER_IDS_VALUE}"
                    ]
                }
            }
        }
    },
    "session": {
        "reset": {
            "mode": "daily",
            "atHour": 4
        }
    },
    "plugins": {
        "entries": {
            "telegram": {
                "enabled": true
            }
        }
    }
}